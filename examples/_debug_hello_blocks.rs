//! Run: cargo run --example _debug_hello_blocks --features _iced_backend
use strimd::{Document, ParseProfile};
use strimd::html::block_cache::BlockRenderCache;

const YOUR_TEXT: &str = include_str!("hello.rs");
fn main() {
    // can't include hello const easily - paste
}
